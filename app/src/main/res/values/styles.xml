<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="popwindow_anim_style">
        <!-- 指定显示的动画xml -->
        <item name="android:windowEnterAnimation">@anim/wrap_slide_vertical_enter_in</item>
        <!-- 指定消失的动画xml -->
        <item name="android:windowExitAnimation">@anim/wrap_slide_vertical_exit_out</item>
    </style>

    <style name="EditTextStyle" parent="android:style/TextAppearance.Holo.Widget.EditText">
        <item name="android:background">@drawable/border_edittext</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:paddingStart">5dp</item>
        <item name="android:paddingEnd">5dp</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:textSize">14dp</item>

    </style>

    <style name="BtnStyle" parent="android:style/TextAppearance.Holo.Widget.Button">
        <item name="android:background">@drawable/round_rect_btn</item>
        <item name="android:textColor">@color/text_white</item>
        <item name="android:minHeight">38dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">14dp</item>
    </style>

</resources>
